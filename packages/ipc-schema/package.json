{
  "name": "@pi-wood/ipc-schema",
  "private": true,
  "version": "0.0.1",
  "description": "全部 IPC 通道的 zod 契约（主/渲染共用唯一事实源）",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test src/engine-rpc.test.ts src/engine-envelope.test.ts src/engine-throttle.test.ts"
  },
  "dependencies": {
    "@pi-wood/plugin-api": "workspace:*",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.3"
  }
}

{
  "name": "@pi-wood/engine",
  "private": true,
  "version": "0.0.1",
  "description": "EngineAdapter 接口 + Pi SDK 实现（渲染层只依赖这一层）",
  "exports": {
    ".": "./src/index.ts",
    "./sdk": "./src/sdk.ts"
  },
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test src/event-bridge.test.ts src/session-tree.test.ts"
  },
  "dependencies": {
    "@pi-wood/ipc-schema": "workspace:*",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "0.84.4",
    "typescript": "^5.6.3"
  }
}

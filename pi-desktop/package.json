{
  "name": "pi-desktop",
  "private": true,
  "version": "0.0.1",
  "description": "PiDesk — 以 Pi 为内核的桌面 Agent 工作台（T0.1 骨架）",
  "scripts": {
    "dev": "pnpm --filter @pidesk/desktop dev",
    "build": "pnpm -r build",
    "package": "pnpm --filter @pidesk/desktop package",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}

{
  "name": "@pidesk/engine",
  "private": true,
  "version": "0.0.1",
  "description": "EngineAdapter 接口 + Pi SDK 实现（渲染层只依赖这一层）",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@pidesk/ipc-schema": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}

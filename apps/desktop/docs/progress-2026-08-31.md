# pi-wood 当前进度快照（2026-08-31）

## 结论

- Phase 2 已完成并通过门禁：桌面端可完成“读取代码 → 修改 → 运行测试 → 浏览器验证”的真实 Provider 闭环。
- T3.1 已进入实现阶段：资源扫描、Skills/Prompt 展示和 `ctx.ui.select/confirm/input` 的主进程到渲染层 Promise 往返链路已落地；真实社区包端到端验收与 `ctx.ui.custom` 降级提示仍待完成。
- 本批次最终门禁通过：`pnpm typecheck`、桌面端专项测试 3/3、`pnpm build`、`git diff --check` 均成功。

## 本批次已完成

### T2.2 Diff 与安全回滚

- 在主进程保存变更 ID、修改前/后内容和目标文件。
- 右栏用 CodeMirror MergeView 展示 Diff，并支持“还原此变更”。
- 回滚前校验文件未被再次修改，避免旧快照覆盖用户的新编辑。
- 路径边界校验拒绝同前缀的项目外路径。
- CRLF 专项测试确认回滚后内容逐字节一致。

证据：`docs/proofs/T2.2/crlf-revert-test.txt`。

### T2.5 工作台联动与布局持久化

- 右栏接入 dockview，支持文件、终端、浏览器和 Diff 面板的标签化布局。
- 工作台布局保存到 settings，重启后可恢复。
- Agent 的 read/edit/bash/browser 工具事件会自动打开对应面板。
- Files、Terminal、Browser、Diff 等重型组件按需懒加载。

### T2.6 Phase 2 门禁

- 真实 Provider 完成 read、edit、bash test、browser navigate、browser read 全链路。
- 独立测试复核通过，总耗时 21.578 秒。
- 验收证据仅记录阶段、状态和耗时，不包含 prompt、回复或凭据。

证据：`docs/proofs/T2.6/acceptance-status.json`。

### T3.1 生态接入（进行中）

- 主进程新增通用 UI 请求队列，支持 `select`、`confirm`、`input`。
- preload 暴露 `ui:request` / `ui:respond`，渲染层新增阻塞式对话框队列。
- 资源管理新增全局/项目 Skills 与 Prompt 模板扫描。
- 设置页新增 Skills 和 Prompt 模板列表。

待完成：

- 安装一个包含扩展与 Skill 的真实社区包并做端到端验收。
- 验证扩展内 `ctx.ui.confirm` 的真实阻塞式 Promise 回传。
- 对 `ctx.ui.custom` 做明确的桌面不支持降级提示。

## 最终验证

| 门禁 | 结果 |
|---|---|
| `pnpm typecheck` | 通过 |
| `pnpm --filter @pi-wood/desktop test` | 3/3 通过 |
| `pnpm build` | 通过 |
| `git diff --check` | 通过 |

测试存在一条非阻塞警告：Node 将 TypeScript 测试文件重新按 ES Module 解析；不影响测试结果，后续可通过统一包模块类型处理。

## 提交边界

本次提交只包含上述实现、测试、证据和计划文档。工作区中的 `.impeccable/`、`.omo/`、`apps/desktop/.impeccable/` 不属于本任务，不纳入提交。

## 下一步

从 T3.1 的真实社区包验收继续；通过后再推进 T3.3 主题全量映射与 T3.4 包市场完整流程。

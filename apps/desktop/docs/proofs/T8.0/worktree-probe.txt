=== T8.0 P2 worktree 探针（git git version 2.34.1，tmp=/tmp/piwood-t80-eJxvD7）===
基线仓库就绪 @d9f3525（node_modules 未跟踪，2MB）
✓ P2-a worktree add 成功 — A=4ms B=4ms
✓ P2-a 工作树不复制未跟踪依赖（node_modules 缺席） — wt-a 大小=0.00MB（主树 node_modules 含 2MB 假依赖）
✓ P2-a .git 是文件而非目录（共享 object 库） — worktree 天然共享 objects，不复制历史
✓ P2-a 两工作树 diff 互不污染 — A diff 13 行 / B diff 13 行
✓ P2-a 两份改动先后回流进主工作树（内容判据） — A=1ms(ok=true) B=2ms(ok=true)，两者均在主树=true
✓ P2-a 回流退出码干净 — 两次 apply 均 0 退出
✓ P2-a 缓解方案有效：--ignore-whitespace 下退出码 0 且内容落盘 — ok=true 含A=true err="" → T8.6 回流固定用 --3way --ignore-whitespace，并以内容/diff 复核而非只看退出码
✓ P2-a 同行冲突不自动合（第二份 apply 失败且原因可读） — 主树还原=true，C=true，D=被拒：Applied patch to 'src/app.js' with conflicts.
✓ P2-a 清理彻底（worktree 列表回到 1 条、.git/worktrees 空、分支已删） — 剩 1 个工作树，残留登记 0
✓ P2-a 清理后主工作树干净（跟踪文件） — 无改动

（跳过真实仓库计时：C:/Users/1/Desktop/pi-wood 不是 git 仓库）

--- P2-b Pi 会话目录按 cwd 编码（worktree 化会令会话分家） ---
  ! Pi SDK 载入失败（Node 版本？须 ≥22）：The requested module 'node:fs' does not provide an export named 'globSync'

--- P2-c 降级路径（每类都必须给出显式文案，不许静默） ---
✓ 降级·非 git 目录：失败且有文案 — 该目录不是 git 仓库，无法为并行对话隔离工作树。请改用 git 仓库，或只开一个对话。
✓ 降级·脏主树：git 本身允许建（记录为「允许但需提示未提交改动会被带走」） — 建成功 → 文案：主工作树有未提交改动，工作树只带提交内容，未提交部分不会出现在并行对话里
✗ 降级·路径大小写不敏感（Windows）：同一路径不同大小写被识别为冲突或有文案 — 拒了：fatal: could not create leading directories of '/TMP/PIWOOD-T80-EJXVD7/WT-A/.git': Read-on
✓ 降级·超长路径：成功或给出可判读文案 — 建成功（Windows 未开长路径限制时可能失败）→ 记录实测可用
✓ 降级·detached HEAD 起点：能建工作树（分支从当前 commit 拉出） — 可行
✓ 降级·含子模块：工作树能建但子模块目录为空（须显式提示） — submodule add=true，worktree=true，子模块空=true → 文案：该仓库含子模块，工作树内的子模块目录默认是空的，需要先 git submodule update --init。并行对话对子模块的改动不会回流主工作树。

=== 结论 ===
15/16 条通过
  ✗ 降级·路径大小写不敏感（Windows）：同一路径不同大小写被识别为冲突或有文案 — 拒了：fatal: could not create leading directories of '/TMP/PIWOOD-T80-EJXVD7/WT-A/.git': Read-on
见失败清单：装载与隔离类失败=No-Go 依据；降级文案类失败=必须补文案后重跑
